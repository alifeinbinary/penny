{
  "description": "Permission configuration for Claude Code in this project",
  "version": "1.0",
  "permissions": {
    "fileOperations": {
      "read": {
        "allowed": true,
        "description": "Full read access to all files in the project"
      },
      "write": {
        "allowed": true,
        "description": "Full write access - can create and modify files"
      },
      "delete": {
        "allowed": true,
        "description": "Full delete access to files and directories"
      }
    },
    "bash": {
      "allowAll": true,
      "description": "Allow all bash commands",
      "blocked": [
        "git *",
        "gh *"
      ],
      "blockedDescription": "Git commands require manual confirmation"
    },
    "webFetch": {
      "allowed": true,
      "allowAll": true,
      "description": "Full web fetch access - can fetch any URL"
    },
    "network": {
      "allowed": true,
      "description": "Full network access for all operations"
    }
  }
}
